# pnpm supply-chain security hardening
# https://pnpm.io/supply-chain-security

# Prevent exotic sources (git deps / direct tarballs) for transitive deps
blockExoticSubdeps=true

# Delay newly-published versions before they can be installed (in minutes)
# 103680 minutes = 72 days
minimumReleaseAge=103680

# Enforce provenance/trust not to get worse between installs
trustPolicy=no-downgrade

